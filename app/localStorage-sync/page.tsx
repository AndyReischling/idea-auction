import LocalStorageSync from '../components/LocalStorageSync';

export default function LocalStorageSyncPage() {
  return (
    <div className="min-h-screen bg-gray-100 py-8">
      <LocalStorageSync />
    </div>
  );
} 